import { Router } from 'express';

const router = Router();

// Aquí irán tus rutas de API
// Ejemplo:
// router.get('/users', (req, res) => {
//     res.json({ message: 'Obtener usuarios' });
// });

export default router;
